extends layout

block content
  h1= title
  p Welcome to #{title}
  form(method="post")
    p
      label(for="login") 
      input(type="text" name='login' id="login")
    
    p
      label(for="password") 
      input(type="password" name='password' id="password")
    
    p 
      input(type="submit")
    