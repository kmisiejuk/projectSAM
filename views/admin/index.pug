extends ../layout

block content
  h1= title
  nav 
      button 
        a(href="/admin/news/add")  dodaj wpis
  table
    tr 
      th id
      th title
      th data dodania
      th akcja
    each item, index in news 
      tr  
        td= index
        td= item.title
        td= item.created 
        td
          a(href=`/admin/news/delete/${item._id}`) Usuń